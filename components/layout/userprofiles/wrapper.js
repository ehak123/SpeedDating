// Doesn't do anything right now.
// Will maybe be useful in displaying full profiles (not during event) later.
export default function ProfileWrapper({ children }) {
    return (
        <>
            {children}
        </>
    )
}